const state={demo:true,eas:[["JetBoomer Core",true],["Scalper X",true],["Trend Sentinel",false],["Risk Guardian",true]]};
const kpis=[["Portfolio Equity","$125,840.20","+$4,812.60 (+3.98%)","positive"],["Today P/L","+$1,284.60","+2.14%","positive"],["Drawdown","4.82%","Limit 12%",""],["AI Confidence","94%","High-conviction setup","info"]];
document.getElementById("kpis").innerHTML=kpis.map(x=>`<div><small>${x[0]}</small><div class="kpi-value">${x[1]}</div><span class="${x[3]}">${x[2]}</span></div>`).join("");
function renderEas(){document.getElementById("eas").innerHTML=state.eas.map((e,i)=>`<div class="ea-row"><div class="ea-name">${e[0]}</div><span class="status ${e[1]?"ok":""}">● ${e[1]?"RUNNING":"PAUSED"}</span><button class="toggle ${e[1]?"on":""}" aria-label="Toggle ${e[0]}" data-ea="${i}"></button></div>`).join("");document.querySelectorAll("[data-ea]").forEach(b=>b.onclick=()=>{const i=+b.dataset.ea;state.eas[i][1]=!state.eas[i][1];renderEas();toast(state.eas[i][0]+(state.eas[i][1]?" resumed":" paused"));});}
renderEas();
document.getElementById("modeBtn").onclick=()=>{state.demo=!state.demo;const b=document.getElementById("modeBtn");b.textContent=state.demo?"DEMO":"LIVE";b.classList.toggle("demo",state.demo);toast("Mode switched to "+(state.demo?"DEMO":"LIVE"));};
document.querySelectorAll("[data-action]").forEach(b=>b.onclick=()=>{const a=b.dataset.action;if(a==="PANIC"&&!confirm("PANIC will block new execution commands in this demo interface. Continue?"))return;toast(a+" command accepted in "+(state.demo?"DEMO":"LIVE")+" mode.");});
document.getElementById("refreshEas").onclick=()=>{renderEas();toast("EA status refreshed.");};
function toast(s){document.getElementById("toast").textContent=s;clearTimeout(window.t);window.t=setTimeout(()=>document.getElementById("toast").textContent="",3500);}
const c=document.getElementById("chart"),ctx=c.getContext("2d");function draw(){const d=devicePixelRatio||1,r=c.getBoundingClientRect();c.width=r.width*d;c.height=r.height*d;ctx.scale(d,d);let w=r.width,h=r.height;ctx.strokeStyle="#202837";ctx.lineWidth=1;for(let y=20;y<h;y+=45){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke()}let pts=[];for(let i=0;i<45;i++)pts.push(h*.62-Math.sin(i*.33)*22-i*2.1+(Math.random()-.5)*12);ctx.beginPath();pts.forEach((y,i)=>{const x=i*w/(pts.length-1);i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.strokeStyle="#2be08a";ctx.lineWidth=2;ctx.stroke();ctx.lineTo(w,h);ctx.lineTo(0,h);ctx.closePath();ctx.fillStyle="rgba(43,224,138,.07)";ctx.fill()}draw();addEventListener("resize",draw);